import { SearchPicturePipe } from './search-picture.pipe';

describe('SearchPicturePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchPicturePipe();
    expect(pipe).toBeTruthy();
  });
});
